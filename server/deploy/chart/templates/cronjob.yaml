{{- $reg := required "registry is required (helm --set registry=...; do not commit it)" .Values.registry }}
apiVersion: batch/v1
kind: CronJob
metadata:
  name: radar-collector
  labels:
    app: federationcoin-radar-collector
spec:
  schedule: {{ .Values.schedule | quote }}
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 3
  jobTemplate:
    spec:
      template:
        metadata:
          labels:
            app: federationcoin-radar-collector
        spec:
          serviceAccountName: {{ .Values.collectorServiceAccount.name }}
          restartPolicy: OnFailure
          securityContext:
            runAsNonRoot: true
            runAsUser: 65532
            fsGroup: 65532
            seccompProfile:
              type: RuntimeDefault
          containers:
            - name: collector
              image: {{ $reg }}/{{ .Values.image.repository }}:{{ .Values.image.tag }}
              imagePullPolicy: {{ .Values.image.pullPolicy }}
              command: ["node", "dist/collector.main.js"]
              securityContext:
                allowPrivilegeEscalation: false
                readOnlyRootFilesystem: true
                capabilities:
                  drop: ["ALL"]
              env:
                - name: RADAR_COLLECTOR_SECRET_NAME
                  value: {{ .Values.collectorSecret.name | quote }}
              volumeMounts:
                - name: tmp
                  mountPath: /tmp
          volumes:
            - name: tmp
              emptyDir: {}
